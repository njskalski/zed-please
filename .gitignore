# Rust build artifacts
target/

# Cargo lock for libraries (keep for binaries/applications)
# Cargo.lock is kept for the extension crate

# Node / tree-sitter tooling
node_modules/

# tree-sitter generated output (regenerated by `tree-sitter generate`)
src/parser.c
src/grammar.json
src/node-types.json
src/tree_sitter/

# Compiled WASM extension (generated by Zed on install)
extension.wasm

# Zed grammar cache
grammars/

# OS
.DS_Store
Thumbs.db
