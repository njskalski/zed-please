# Rust build artifacts
target/

# Node / tree-sitter tooling
node_modules/

# tree-sitter generated output that Zed regenerates itself
# src/parser.c and src/tree_sitter/ are committed so Zed can compile the grammar
src/grammar.json
src/node-types.json

# Compiled WASM extension (generated by Zed on install)
extension.wasm

# Zed grammar cache
grammars/

# OS
.DS_Store
Thumbs.db
